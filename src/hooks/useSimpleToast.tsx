'use client'

// Alias de compatibilidad: los consumidores destructuran { showToast,
// ToastContainer }, exactamente lo que devuelve el hook global.
export { useToast as useSimpleToast } from './useToast'
